<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.SampleDao">
    <select id="findAllUsers" resultType="java.util.Map">
        select * from testa;
    </select>

    <select id="findUserByIds" parameterType="java.util.Map" resultType="java.util.Map">
        select * from testa;
    </select>
    
      <select id="findUserById" parameterType="java.util.Map" resultType="java.util.Map">
        select * from testa;
    </select>

</mapper>